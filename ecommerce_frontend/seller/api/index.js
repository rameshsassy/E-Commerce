/**
 * Vercel serverless entry when the project Root Directory is ecommerce_frontend/frontend.
 * Installs and bundles the Express app from the repository root (see vercel.json installCommand).
 */
export { default } from '../../../api/index.js';
